package com.gurch.sandbox.policies;

/** Supported policy input field data types for rule authoring and evaluation. */
public enum PolicyInputFieldDataType {
  STRING,
  NUMBER,
  BOOLEAN,
  DATE,
  DATETIME,
  JSON
}
