package com.gurch.sandbox.documenttemplates;

/** Supported field kinds discovered during document template introspection. */
public enum DocumentTemplateFormFieldType {
  TEXT,
  MULTILINE_TEXT,
  CHECKBOX,
  RADIO,
  SELECT,
  UNKNOWN
}
