package com.gurch.sandbox.requests;

/** Enumeration of possible request statuses. */
public enum RequestStatus {
  DRAFT,
  SUBMITTED,
  IN_PROGRESS,
  AWAITING_SIGNATURE,
  AWAITING_PROCESSING,
  COMPLETED,
  REJECTED
}
