package com.gurch.sandbox.requests.activity.dto;

/** Canonical event types for request activity timeline entries. */
public enum RequestActivityEventType {
  STATUS_CHANGED,
  TASK_ASSIGNED,
  TASK_COMPLETED
}
